import { darkColors } from "./semantic/colors";
import { fontFamily, fontSizes } from "./semantic/text";

import { darkPalette } from "./tokens/palette";
import { radii } from "./tokens/radii";

export const darkTheme = {
  fontFamily,
  fontSizes,
  colors: darkColors,
  palette: darkPalette,
  radii,
};
